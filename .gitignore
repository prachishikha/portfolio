# Resume PDF — deploy via Netlify drag-and-drop, not git
*.pdf

# Profile photos — deploy via Netlify drag-and-drop, not git
images/*.png
images/*.jpg
images/*.jpeg
images/*.webp

# OS files
.DS_Store
